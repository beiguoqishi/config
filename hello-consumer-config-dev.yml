spring:
  application:
    name: hello-consumer
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
#        注册到registory才能被turbine发现
        register: true
  zipkin:
    base-url: http://localhost:9411
    sender:
      type: web
  sleuth:
    web:
      enabled: true
    sampler:
      probability: 1.0
server:
#  端口号不能大于10000，不然服务发现不了，奶奶的
  port: 8001
logging:
  level:
    org:
      example: DEBUG
feign:
  hystrix:
    enabled: true
turbine:
  app-config: hello-consumer
  aggregator:
    cluster-config:
      - default
  cluster-name-expression: new String("default")
management:
  endpoints:
    web:
      exposure:
        include: '*'
username: England
mysql-password: "{cipher}b2cc1cd9d3b873f01ea076ceb92c750cc8759bd678fb35056c46211922bbdb35"
#eureka:
#  client:
#    serviceUrl:
#      defaultZone: http://node1:8091/eureka/,http://node2:8092/eureka/,http://node3:8093/eureka/
